import { CenteredContainer } from '../Common/Common.styles';

export const ExampleBlock = () => {
    return (
        <CenteredContainer>
            <h2>Have an idea? Let's build a landing for it!</h2>
            <p><i>It's easy as hell</i></p>
        </CenteredContainer>
    );
}